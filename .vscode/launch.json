{
    "configurations": [
        {
            "command": "npm i && npm run prebuild && npm run start:dev",
            "name": "Nestjs start",
            "request": "launch",
            "type": "node-terminal"
        },
    ]
}